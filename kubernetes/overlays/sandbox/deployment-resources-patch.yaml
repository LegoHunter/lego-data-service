apiVersion: apps/v1
kind: Deployment
metadata:
  name: lego-data-service-deployment
spec:
  template:
    spec:
      containers:
        - name: lego-data-service
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "250m"
