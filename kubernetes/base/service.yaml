apiVersion: v1
kind: Service
metadata:
  name: lego-data-service
spec:
  type: ClusterIP
  selector:
    app: lego-data-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
